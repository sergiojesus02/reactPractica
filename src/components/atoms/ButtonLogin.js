// A new component based on Button, but with some override styles
import { ButtonLog } from "../assets/styles/Styles";

const ButtonLogin = () => {
  return (
        <ButtonLog>Sign Up</ButtonLog>
  );
};

export default ButtonLogin;
